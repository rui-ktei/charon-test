# Subject for the watch-list-scoping live run

Raised to take one pull request through checks running, checks concluding and a merge, so that
the split between `Waiting` and `Blocked`, the recorded finish time and the sentence a row
renders are proven against real GitHub rather than against a scripted one.

Nothing in this file is read by anything.

A second commit, to restart the checks so the waiting state is observed while they run.
